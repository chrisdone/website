name: Generate

on:
 push:
  branches:
   - master

jobs:
  generate:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2

      - name: hell website.hell
        run: |
          time hell website.hell
          sudo rsync -r webroot/* /var/www/html/
